@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        // Redirection vers login si on essaie d'accéder à une page protégée
        Navigation.NavigateTo("login");
    }
}